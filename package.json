{
  "name": "polizas-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm --dir backend install && pnpm --dir frontend install && pnpm --dir frontend run build && cp -r frontend/dist/. backend/public/",
    "start": "node backend/server.js"
  }
}
